import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  bookItem: {
    width: '100%',
    height: '100%',
    borderRadius: 16,
  },
  currentBookInfo: {gap: 4},
  textCenter: {
    textAlign: 'center',
  },
});
