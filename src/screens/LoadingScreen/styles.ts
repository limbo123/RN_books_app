import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  heartsBg: {
    position: 'absolute',
  },
  logo: {
    width: '100%',
    height: 52,
    marginBottom: 12,
  },
  mainContent: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 58,
  },
  loadingBar: {marginTop: 40},
});
