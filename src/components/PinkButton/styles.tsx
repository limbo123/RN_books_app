import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  readNowButton: {
    backgroundColor: '#D04E9D',
    paddingVertical: 12,
    marginHorizontal: 16,
    borderRadius: 30,
    alignItems: 'center',
    marginTop: 20,
  },
});
